# __TITLE__

## Rule of success

Rule of success: __SUCCESS__

This gem teaches how **__TITLE__** is done. FlowSteps refine the current
candidate. The harness still requires the declared named output bundle.
Intelligence may draft. It must not set `ok`, `branch`, or
`cycle`.

The judge reads **Rule of success** only. Each FlowStep section below is
that step's prompt. It is not a canvas node. It does not set `ok`.

- Milestone: `__MID__`
- Primary output kind: `__KIND__`
- Gem: `references/__MID__.md`
- Worker: `__WORKER__` (looks at **Rule of success** and writes the receipt; the model may not set `ok`)
__JUDGE_LINE__

Follow the FlowStep table as a guide. If the preferred tool fails,
recover like a normal agent and use that FlowStep's section as the
prompt. Judge PASS commits the current result to `chosen-output.json`.
Missing or invalid declared output members still BLOCK.

## FlowSteps

__FLOWSTEP_SECTIONS__
