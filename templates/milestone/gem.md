# __TITLE__

Rule of success: __SUCCESS__

This gem teaches how **__TITLE__** is done. The harness still requires
the asset. Intelligence may draft. It must not set `ok`, `branch`, or
`cycle`.

- Milestone: `__MID__`
- Asset: `__KIND__`
- Gem: `references/__MID__.md`
- Worker: `__WORKER__` (looks at this gem and writes the receipt; the model may not set `ok`)
__JUDGE_LINE__

Follow the FlowStep table as a guide. If the preferred tool fails,
recover like a normal agent. Missing the asset is still BLOCK.
