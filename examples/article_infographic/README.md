# Sample: article infographic

This is a real audit of a seven-page article-to-carousel skill (URL or text in, 1080x1350 cards out).

Generated by:

```powershell
python scripts/audit_harness.py --target <article-infographic-skill>
```

The published artifact is [planning/m8m-flowchart.md](planning/m8m-flowchart.md): one JPEG chart, a FlowStep table, a **for** milestone (`assets_bound` walks the pages ledger), and a **judge** milestone (`cards_rendered` until worker ok). GitHub does not use mermaid.
