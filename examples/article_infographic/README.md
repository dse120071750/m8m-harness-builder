# Sample: article infographic

This is a real audit of a seven-page article-to-carousel skill (URL or text in, 1080x1350 cards out).

Generated by:

```powershell
python scripts/audit_harness.py --target <article-infographic-skill>
```

The published artifact is [planning/m8m-flowchart.md](planning/m8m-flowchart.md) plus [planning/m8m-flowchart.jpg](planning/m8m-flowchart.jpg): two panels (milestone canvas with human labels, then FlowSteps inside `cards_rendered`), plus the FlowStep table. GitHub does not use mermaid. The builder rewrites both files after generate and after every step edit.
